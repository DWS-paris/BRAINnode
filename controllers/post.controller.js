/* 
Imports
*/
    const Models = require('../models/index');
//

/* 
CRUD methods
*/
    const createOne = req => {
        return new Promise( (resolve, reject) => {

        })
    }

    const readAll = req => {
        return new Promise( (resolve, reject) => {

        })
    }

    const readOne = req => {
        return new Promise( (resolve, reject) => {

        })
    }

    const updateOne = req => {
        return new Promise( (resolve, reject) => {

        })
    }

    const deleteOne = req => {
        return new Promise( (resolve, reject) => {

        })
    }
//

/* 
Export controller methods
*/
    module.exports = {
        readAll,
        readOne,
        createOne,
        updateOne,
        deleteOne
    }
//